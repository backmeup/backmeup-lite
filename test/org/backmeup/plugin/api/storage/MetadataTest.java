package org.backmeup.plugin.api.storage;

public class MetadataTest {
	
	/*
	private static String TEST_JSON =
		"{ \"dcterms:author\":\"Rainer\", " +
	      "\"dcterms:title\":\"A title for this data object\", " +
		  "\"foaf:thumbnail\":\"http://www.example.com/logo.jpg\" }";
	
	
	private static Metadata meta = new Metadata();
	
	static {
		meta.addProperty("dcterms:title", "A title for this data object");
		meta.addProperty("dcterms:author", "Rainer");
		meta.addProperty("foaf:thumbnail", "http://www.example.com/logo.jpg");		
	}
	
	@Test
	public void testToJSON() {
		String json = Metadata.toJSON(meta);
		Assert.assertEquals(TEST_JSON, json);
	}
	
	@Test
	public void testFromJSON() {
		Metadata parsed = Metadata.fromJSON(TEST_JSON);
		Assert.assertTrue(parsed.containsKey("dcterms:title"));
		Assert.assertTrue(parsed.containsKey("dcterms:author"));
		Assert.assertTrue(parsed.containsKey("foaf:thumbnail"));
	}
	*/
	
}
